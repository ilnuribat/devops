- name: Create directory
  file:
    path: /tmp/lib/guov
    state: directory
- name: git clone
  git:
    repo: ssh://git@bitbucket.org/guov/stimul.git
    dest: /tmp/lib/guov/stimul
    depth: 1
    version: new-back-test
  register: git_result
- name: last commit
  set_fact:
    last_commit: "{{ git_result['after'][0:8] }}"
